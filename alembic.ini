# Alembic configuration. No sqlalchemy.url here on purpose: the URL is built at
# runtime (config.DATABASE_URL, else app.utils.user_db.DB_PATH) by
# app/db/migrations/env.py, or passed programmatically by app/db/migrate.py.

[alembic]
script_location = app/db/migrations
prepend_sys_path = .
